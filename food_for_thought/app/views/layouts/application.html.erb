<!DOCTYPE html>
<html>
  <head>
    <link href="https://fonts.googleapis.com/css?family=Marck+Script" rel="stylesheet">
    <title>Food For Thought</title>
    <link rel="icon" href="http://downloads.andyroid.net/website/v2//wp-content/uploads/2015/07/Slice-Pizza-icon.png">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="background">
      <h1 class="heading">Food for Thought</h1>
      <div class="nav">
        <nav>
          <a href="/" class="nav-link">Home</a>
          <% if session[:user_id] %>
            <a href="/posts" class="nav-link">Feed</a>
            <%= link_to("Profile", user_path(session[:user_id], :class => "nav-link")) %>
            <%= link_to("Edit Profile", edit_user_path(session[:user_id], :class => "nav-link")) %>
            <%= link_to("Sign Out", session_path(session[:user_id]), method: :delete, :class => "nav-link") %>
          <% else %>
            <a href="/users/new" class="nav-link">Sign Up</a>
            <a href="/sessions/new" class="nav-link">Sign In</a>
          <% end %>
        </nav>
  </div>

      <% if flash[:notice] %>
        <div class="notice">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="alert">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
